多态是Java面向对象的三大特征之一：
多态通过统一的接口或父类定义的抽象方法来使得不同实现接口或继承抽象父类的对象各自完成相应的动作。
比如List接口下的ArrayList和LinkedList
方便在调用时通过统一的接口指向不同的对象来完成响应的业务，达到解耦和灵活的效果。
List<String> list = new ArrayList<>();
能点出来什么看引用类型
List接口引用类型指向ArrayList对象，通过后者对象实现的前者接口。
两个口诀：
1.多态成员变量：编译运行看左边
2.多态成员方法：编译看左边，运行看右边